/**
 * RevenueCat Constants
 * Default values and constants used across the package
 */

export const DEFAULT_ENTITLEMENT_IDENTIFIER = 'premium';

export const REVENUECAT_LOG_PREFIX = '[RevenueCat]';

export const CREDITS_PRODUCT_IDENTIFIERS = ['credits', 'consumable'] as const;